name: Tests with Report

on:
  pull_request:
    branches: [ main, lab2 ]
      
jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
    - name: Build test
      run: ./PPO_f/gradlew :BL:test :DB:test
      
