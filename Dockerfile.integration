FROM eclipse-temurin:21-jdk

WORKDIR /app

COPY PPO_f /app/PPO_f
WORKDIR /app/PPO_f

ENV POSTGRES_HOST=postgres \
    POSTGRES_PORT=5432 \
    POSTGRES_USER=testuser \
    POSTGRES_PASSWORD=testpassword \
    POSTGRES_DB=testdb

RUN ./gradlew build

ENTRYPOINT ["./gradlew", ":app_cli:test"]
